{
	"name": "convoy",
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"dev": "dotenv-run turbo run dev",
		"lint": "turbo run lint",
		"format": "prettier --write \"**/*.{ts,tsx,md}\"",
		"check-types": "turbo run check-types"
	},
	"devDependencies": {
		"prettier": "^3.7.4",
		"turbo": "^2.10.7",
		"typescript": "5.9.2"
	},
	"engines": {
		"node": ">=18"
	},
	"devEngines": {
		"packageManager": {
			"name": "npm",
			"version": "11.13.0"
		}
	},
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"dependencies": {
		"dotenv-run": "^0.0.1"
	}
}
